import React from "react";
import { describe, test, expect } from "@jest/globals";

describe("API test FRONT", () => {
  test("getListNews", async () => {
    /*    fetch(process.env.NEXT_PUBLIC_APP_FETCH + "/api/News/get_all_news").then(
      (data) => {
        console.log("jhgfd", data);
      },
    );*/
    /*        console.log("res", result);
        expect(result.name).toBe('Leanne Graham');*/
  });
});
